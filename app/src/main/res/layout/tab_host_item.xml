<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:id="@+id/layout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/iv_icon"
            android:layout_width="@dimen/tab_icon_size"
            android:layout_height="@dimen/tab_icon_size"
            android:layout_marginTop="4dp"
            android:src="@drawable/icon_tab_host_user_select" />

        <TextView
            android:id="@+id/tv_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/tab_padding"
            android:textColor="@drawable/tab_host_text_color"
            android:textSize="11sp" />

    </LinearLayout>

    <ImageView
        android:visibility="gone"
        android:id="@+id/iv_tip"
        android:layout_width="@dimen/tab_dot_size"
        android:layout_height="@dimen/tab_dot_size"
        android:layout_alignParentTop="true"
        android:layout_marginTop="6dp"
        android:layout_marginLeft="2dp"
        android:layout_toRightOf="@id/layout"
        android:scaleType="centerInside"
        android:src="@drawable/icon_red_tip" />
</RelativeLayout>